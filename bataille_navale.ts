import {Boat} from "./classes/boat";
import {Player} from "./classes/player";
import {HumanPlayer} from "./classes/player";
import {AI} from "./classes/player";
import{Map} from "./classes/map"
import { player1 } from "./classes/player";
import { player2 } from "./classes/player";










//player1.shoot(player2,1,0);

player2.shoot(player1);
player2.shoot(player1);
player2.shoot(player1);
player2.shoot(player1);
player2.shoot(player1);
player2.shoot(player1);
player2.shoot(player1);
player2.shoot(player1);

//player1.shoot(player2,2,5);
//console.log(player1.map1.map);

/*const readline = require('readline');

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

rl.question(' ', (answer) => {
  // TODO: Log the answer in a database
  console.log(`Thank you for your valuable feedback: ${answer}`);

  rl.close();
});*/
